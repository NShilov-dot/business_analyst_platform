# RUNTIME deps for services/asr — documentation only. modal_app.py's `image`
# definition is the source of truth for what actually ships in the deployed
# container; keep this file in sync by hand if that changes.
torch==2.10.*
torchaudio==2.10.*
transformers==5.*
hydra-core
omegaconf
soundfile
sentencepiece
pyannote.audio
huggingface_hub
httpx
fastapi[standard]
piper-tts
